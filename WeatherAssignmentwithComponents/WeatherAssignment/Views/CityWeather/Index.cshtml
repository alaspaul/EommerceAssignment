
@model List<CityWeather>;


        <div class="margin-top-200 margin-left">
            <div class="flex">
                @foreach (CityWeather city in Model)
                {
                    @await Component.InvokeAsync("CityWeather", new { city = city })
                }

            </div>
        </div>
